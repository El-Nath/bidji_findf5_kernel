--- net/ceph/osd_client.c
+++ net/ceph/osd_client.c
@@ -1718,8 +1715,6 @@
 				dout("osdc_start_request failed map, "
 				     " will retry %lld\n", req->r_tid);
 				rc = 0;
-			} else {
-				__unregister_request(osdc, req);
 			}
 			goto out_unlock;
 		}
